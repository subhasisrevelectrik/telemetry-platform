# CAN Telemetry Platform - Environment Configuration
#
# SETUP:
#   1. Copy: copy .env.example .env
#   2. Fill in values (or just use `aws login` and skip AWS keys)
#   3. .env is gitignored - safe from accidental commits
#
# RECOMMENDED: Use `aws login` for AWS auth (no static keys needed)

AWS_ACCESS_KEY_ID=your-access-key-here
AWS_SECRET_ACCESS_KEY=your-secret-key-here
AWS_DEFAULT_REGION=us-east-1

# Backend
LOCAL_MODE=true

# Frontend (set when deploying to AWS)
# VITE_API_URL=https://your-api-gateway-url
