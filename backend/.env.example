# Backend Environment Configuration
#
# Copy this file: cp .env.example .env
# NEVER commit .env — it is gitignored.

# Set to true to use local Parquet files, no AWS needed
LOCAL_MODE=true
LOCAL_DATA_DIR=./data/decoded

# Cloud (Athena) mode — fill these in when deploying
# ATHENA_DATABASE=telemetry_db
# ATHENA_RESULTS_BUCKET=telemetry-athena-results-<your-account-id>
# S3_DATA_BUCKET=telemetry-data-lake-<your-account-id>
AWS_REGION=us-east-1
