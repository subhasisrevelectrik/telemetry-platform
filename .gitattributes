# Default: normalise line endings in the index to LF
* text=auto eol=lf

# Shell scripts MUST have LF on all platforms (bash breaks with CRLF)
*.sh        text eol=lf
*.service   text eol=lf

# Windows batch scripts keep CRLF
*.bat       text eol=crlf
*.cmd       text eol=crlf

# Python, YAML, JSON, Markdown — LF everywhere
*.py        text eol=lf
*.yaml      text eol=lf
*.yml       text eol=lf
*.json      text eol=lf
*.md        text eol=lf
*.toml      text eol=lf

# Binaries — no line ending conversion
*.parquet   binary
*.png       binary
*.jpg       binary
*.zip       binary
